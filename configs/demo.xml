<?xml version="1.0"?>
<root main_tree_to_execute="BehaviorTree">
    <!-- ////////// -->
    <BehaviorTree ID="BehaviorTree">
        <Sequence>
            <SequenceStar>
                <Action ID="CheckStatus"/>
            </SequenceStar>
            <Action ID="CheckFlags"/>
            <Action ID="SayHello"/>
        </Sequence>
    </BehaviorTree>
    <!-- ////////// -->
    <TreeNodesModel>
        <Action ID="CheckFlags"/>
        <Action ID="CheckStatus"/>
        <Action ID="SayHello"/>
    </TreeNodesModel>
    <!-- ////////// -->
</root>

